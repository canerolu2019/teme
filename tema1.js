// Function that takes 2 paramiters (values)
function isEqual(first, second) {
    // If block compares if the values are equal
    if (first === second) {
      // If the condition from if checking is true then return true
      return true
    } else { 
      // If the condition from if is false the return false
      return false;
    }
  }